<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.siegfrield.hakuna_matata">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".service.manager.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.HostActivity"
            android:label="@string/title_activity_host"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.RetrofitDemoActivity" />
        <activity
            android:name=".ui.activity.LinearLayoutDemoActivity"
            android:label="@string/title_activity_linear_layout_demo"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>